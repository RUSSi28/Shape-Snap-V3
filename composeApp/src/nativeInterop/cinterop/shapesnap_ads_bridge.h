#import <UIKit/UIKit.h>

NS_ASSUME_NONNULL_BEGIN

/** アプリ側（iosApp の .m）で実装。Kotlin からは UIView のみ参照する。 */
UIView *shapesnap_ads_create_test_banner(void);
void shapesnap_ads_start_if_needed(void);

NS_ASSUME_NONNULL_END
